# 이슈 대응 로그 (SM 실습용)

실제 SM(시스템 운영/유지보수) 업무에서는 "장애/이슈 발생 → 원인 분석 → 수정 → 재발 방지"의
흐름으로 이슈를 기록하고 처리합니다. 아래 형식으로 하나씩 채워보세요.
채워진 로그는 자소서 "직무와 관련된 경험" 문항의 좋은 소재가 됩니다.

---

## BUG-001: 이체 중 실패 시 자금 정합성 문제 (예시, 이미 코드에 남겨둔 이슈)

- **증상**: `Bank.transfer()`에서 출금(withdraw)은 성공했는데 입금(deposit) 단계에서
  예외나 오류가 발생하면, 돈이 출금 계좌에서는 빠졌는데 입금 계좌에는 들어가지 않는
  상태가 될 수 있음.
- **원인 분석**: 출금과 입금이 하나의 트랜잭션으로 묶여있지 않고 순차적으로만 처리됨.
  중간에 실패가 나도 이미 처리된 출금을 되돌리는 로직(롤백)이 없음.
- **재현 방법**: (직접 실습) `to` 계좌번호를 존재하지 않는 값으로 넣고 이체를 시도해서
  `from` 계좌 잔액이 실제로 줄었는지, 거래내역에는 어떻게 남는지 확인해보기.
- **수정 방법**: (직접 작성해보기) 출금 전에 입금 대상 계좌가 존재하는지 먼저 검증하거나,
  실패 시 출금을 되돌리는 보정 로직을 추가.
- **재발 방지**: (직접 작성해보기) 이체처럼 여러 단계로 이뤄진 작업은 원자적으로 처리되도록
  설계 원칙을 세우고, 유사 로직에도 동일하게 점검.

---

## BUG-002: (직접 찾아서 채워보기)

- 힌트: 음수 금액 입력, 존재하지 않는 계좌 조회, 잔액보다 큰 금액 출금 등
  예외 상황을 하나씩 직접 테스트해보고 이상하게 동작하는 부분을 찾아보세요.

- **증상**:
- **원인 분석**:
- **재현 방법**:
- **수정 방법**:
- **재발 방지**:

---

## AI 협업 로그 (AI 활용 문항 소재용)

막힌 부분을 AI(Claude 등)에게 질문하며 해결한 과정을 간단히 기록해두세요.
자소서 "AI와의 협업을 통해 문제를 해결한 사례" 문항에 그대로 쓸 수 있습니다.

- 질문한 내용:
- AI가 제안한 방향:
- 실제로 적용/수정한 부분:
- 배운 점:
